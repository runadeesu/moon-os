# moon OS ロードマップ

このファイルは moon OS の開発進捗を追跡するためのマスタードキュメントです。
セッションをまたいで開発を継続する際は、必ずこのファイルを最初に確認してください。

## 開発方針

- 学習用のトイOSではなく、実際に動作する完成品を目指す。
- カーネルは Rust (no_std) で実装。必要な箇所のみアセンブリを使用。
- ブートローダーは独自実装せず、実績のある OSS ブートローダー [Limine](https://github.com/limine-bootloader/limine) を採用する
  (Limine Boot Protocol)。理由: 64bit long mode への遷移、UEFI/BIOS 両対応、フレームバッファ取得、
  メモリマップ取得などをブートローダーが提供してくれるため、カーネル自体の独自性・品質に開発リソースを
  集中できる。EXE/APK 互換レイヤーも同様に「car-from-scratch」ではなく、独自実装 + 適切な OSS 連携。
- 各マイルストーンごとに実機/QEMU で動作確認してからコミットする。

## マイルストーン

### M0 — プロジェクト基盤 (完了)
- [x] リポジトリ構造の設計
- [x] Rust no_std カーネルクレートの雛形
- [x] Limine ブートローダーとの連携 (limine.conf, Limine Boot Protocol の自前バインディング)
- [x] シリアル出力 (COM1) によるログ
- [x] GDT / TSS 初期化
- [x] IDT / 例外ハンドラ (#DE, #PF, #GPF など)
- [x] フレームバッファコンソール (文字描画) でブートバナー表示
- [x] ISO 生成 + QEMU 起動スクリプト (build.sh / run.sh)
- [ ] CI (GitHub Actions) でビルド確認

### M1 — メモリ管理 (完了)
- [x] 物理メモリアロケータ (Bitmap 方式、Limineメモリマップの `Usable` 領域のみ使用)
- [x] ページング管理 (Limineが渡した既存ページテーブルへの独自マッピング追加。
      `map` / `unmap` / `translate` を実装。HHDM経由でページテーブルへアクセス)
- [x] カーネルヒープアロケータ (`#[global_allocator]`、自前実装のfirst-fitフリーリスト方式)
- [x] Rust `alloc` クレートの活用開始 (`Vec` 等の動作をQEMU実機で確認済み)

補足: `BootloaderReclaimable` 領域は今回あえて未使用のまま (Limine自身のページテーブル等が
そこに存在し、まだ独自ページテーブルに切り替えていないため)。将来、独自のトップレベルページ
テーブルを構築してCR3を切り替えるマイルストーンで回収する予定。

### M2 — 割り込み・マルチタスク (基本部分 完了)
- [x] PIC 初期化 (8259 を IRQ0-15 → ベクタ32-47 にリマップ。Local APIC/I/O APIC は SMP対応時に着手)
- [x] PIT タイマー割り込み (100Hz)
- [x] プロセス/スレッド構造体 (`kernel/src/sched.rs` の `Task`)
- [x] コンテキストスイッチ — 「suspendされたタスクの状態 = 自分のスタック上に載った TrapFrame」
      という設計にすることで、専用のアセンブリ切り替えコードなしに `iretq` の仕組みそのものを
      流用して実現 (M2開発中に発見した重要な罠: x86-64 の `iretq` は特権レベルが変わらない
      場合でも RSP/SS を含む5フィールドを必ずポップする。手動構築したフレームに3フィールドしか
      用意していないと、直後のゴミデータを SS として誤読し `#GP` で落ちる。詳細はコード内コメント参照)
- [x] ラウンドロビンスケジューラ (`sched::on_timer_tick`)
- [ ] APIC (Local/IO) — SMP・優先度制御が必要になった時点で 8259 から移行
- [ ] システムコール (SYSCALL/SYSRET) — ユーザーモード導入時に着手

### M3 — 入力デバイス (PS/2 完了)
- [x] PS/2 キーボードドライバ (スキャンコードセット1 → ASCII、シフト/CapsLock状態は未実装)
- [x] PS/2 マウスドライバ (標準3バイトパケット、IntelliMouseホイールは未対応)
- [ ] USB スタック (xHCI) → USB キーボード/マウス/ストレージ

### M4 — ファイルシステム (VFS/RAMFS/AHCI 完了)
- [x] VFS (仮想ファイルシステム層) — 最小限の実装 (`kernel/src/fs/`)。マウントポイントは
      未対応で、今は RAMFS 一枚のみ
- [x] RAMFS / initrd (`kernel/src/fs/ramfs.rs`)
- [x] PCI バス列挙 (`kernel/src/drivers/pci.rs`、レガシー0xCF8/0xCFC方式)
- [x] AHCI (SATA) ドライバ (`kernel/src/drivers/ahci.rs`) — HBA/ポート初期化、
      ATA IDENTIFY、ATAPI PACKET(SCSI READ(10)) 経由でのセクタ読み込みに対応。
      起動に使ったISOイメージ自体からセクタ16(ISO9660 PVD)を実際に読み取り、
      "CD001"シグネチャの一致を確認済み — PCI→AHCI→ATAPIの全経路が実機で動作する証明
  - 補足: MMIOレジスタは`read_volatile`/`write_volatile`必須という罠を開発中に踏んだ
    (素の参照読み書きだとコンパイラがポーリングループを最適化で消してしまい、
    ハードウェア完了を待たずタイムアウトする)。詳細はコード内コメント参照
- [x] `ata_read_sector`/`ata_write_sector` (READ/WRITE DMA EXT) を追加し、
      `fs::persist`(`kernel/src/fs/persist.rs`)がRAMFS全体を実SATAディスクへ
      スナップショット保存/復元 — ブートを跨いでファイル・アカウントが
      実際に残ることをQEMUの2回起動で確認済み(詳細は下記「現在の状態」参照)。
      ただし本物のブロック単位ファイルシステム(inode/空き容量管理/単一ファイルの
      部分書き換え)ではなく、毎回ツリー全体を書き直す素朴なスナップショット方式 —
      この制約はコード内コメントに明記
- [ ] 独自ファイルシステム moonFS、または実績ある FAT32/ext2 実装との連携
      (今回はISO9660パーサまでは実装せず、生セクタ読み込みの証明に留めた)
- [x] ファイルマネージャー (GUI アプリ) — 完了 (M10参照)

### M5 — GUI / ウィンドウシステム (基本部分 完了)
- [x] コンポジタ / ウィンドウマネージャー (`kernel/src/gui/`) — zオーダー管理、
      クリックでフォーカス/最前面化、タイトルバードラッグでの移動に対応
- [x] 描画プリミティブ (矩形、テキスト。画像は未対応) — `framebuffer.rs` にピクセル単位の
      `fill_rect`/`draw_char_at`/`draw_str_at` を追加
- [x] イベントループ (マウス/キーボード入力の配送) — PS/2ドライバの割り込みハンドラから
      直接GUIへイベントを送り、フォーカスされているウィンドウへキー入力をルーティング
- [x] タスクバー (`kernel/src/gui/taskbar.rs`)
- [x] 設定アプリ相当 (`kernel/src/gui/widgets/settings.rs`) — メモリ/タスク数/稼働時間を
      ライブ表示する読み取り専用ダッシュボード
- [x] ターミナルアプリ相当 (`kernel/src/gui/widgets/terminal.rs`) — help/clear/uptime/mem/echo
      の組み込みコマンドを持つスクロールバック付きターミナル
- 補足: まだユーザーモード/ELFローダー(M7/M8)が無いため、上記の「アプリ」は独立した
  プロセスではなく、ウィンドウマネージャーがホストするカーネル内蔵ウィジェットとして実装。
  実際のアプリプロセスの起動は M7 のアプリ基盤が整ってから
- [ ] 画像描画 (ビットマップ/PNG等)
- [ ] ダブルバッファリング (現状は毎回フレームバッファに直接描画するため、
      再描画のたびにわずかなちらつきが発生し得る)
- [x] デスクトップの見た目刷新 (ダーク×月/宇宙テーマ) — `framebuffer.rs` に
      `blend_rect`(実アルファブレンド、既存ピクセルを読み戻して合成)と
      `fill_circle`(整数sqrtによるスキャンライン走査)を追加。
      `gui/desktop.rs` で手続き型の壁紙(夜空グラデーション、xorshift PRNGの
      固定星空、月、山のシルエット2層)を生成。下部タスクバーは左サイドの
      ドック (`gui/dock.rs`) に置き換え、クリックでウィンドウ切り替え可能に。
      上部に `gui/topbar.rs`(ロゴ/実CMOS RTCからのライブ時計/ネット状態)、
      右上に `gui/desktop_widgets.rs`(実日付カレンダー — Zellerの公式で
      曜日計算、システムモニター)の半透明パネルを追加。新設の
      `kernel/src/drivers/rtc.rs` がCMOSポート0x70/0x71から実時刻を読み取る
      (BCD/12時間表記の変換、update-in-progress競合への対処込み)
- [x] UI多言語対応 (`kernel/src/i18n.rs`) — 設定ウィンドウの「Lang」行クリックで
      英語/日本語/スペイン語/フランス語を切り替え、ウィンドウタイトルやラベルが
      即座に翻訳されて再描画される(QEMU実機でEnglish→Japaneseへの切り替えを確認済み)。
      日本語は`font_hiragana.rs`(dhepper/font8x8由来、パブリックドメインの
      ひらがな96字。実際にGitHubから取得しプログラムでパース、手打ち転記ミスを回避)で
      全てひらがな表記 — 漢字/カタカナのグリフは未実装のため、本来カタカナで書く
      外来語(システム、タスク等)もひらがなで代用(漢字を習う前の日本語教材と同じ方式)。
      「Terminal」やブランド名「moon OS」/「NET」は全言語で共通のまま
- [x] テキスト描画のソフト化 — `framebuffer::draw_char_at` が1bitグリフの縁に
      低アルファのハローを追加する2パス方式で、擬似アンチエイリアスによりカクカクした
      印象を軽減(コードページ的encoding: バイト0x80+でひらがなテーブルを参照する
      `draw_glyphs_at`も追加、既存の`draw_str_at`(ASCII専用`&str`)とは別経路)
- [x] 未来感のあるUIクロム — `framebuffer::glow_border`(同心円状の半透明アウトライン)
      によるネオングロー効果をフォーカスウィンドウ・ドック(アクティブアイコン)・
      カレンダーの「今日」・システムモニター等に適用。`draw_corner_brackets`で
      フォーカスウィンドウの四隅にHUD風Lブラケット、壁紙全体に薄いシアン系
      スキャンラインを追加

### M6 — ネットワーク (IPv4基本スタック 完了)
- [x] NIC ドライバ — RTL8139 (`kernel/src/drivers/rtl8139.rs`)。レジスタ数が少なく
      RXがディスクリプタリング無しの単純なリングバッファ方式のため、最初のNICドライバとして選定。
      ポーリング方式 (IRQルーティングは未実装、AHCIと同じ判断)
- [x] Ethernet / ARP / IPv4 / ICMP / UDP を自前実装 (`kernel/src/net/`)
- [x] DHCP クライアント (DISCOVER→OFFER→REQUEST→ACK) — QEMU SLIRPから実際にリース取得を確認
- [x] DNS (簡易・1発クエリのみ、キャッシュ/リトライ無し) — 実際に`example.com`の名前解決に成功
      (このサンドボックス環境がSLIRP経由で実インターネットに到達できることも同時に確認)
- [ ] TCP/IP スタック — TCPの状態機械(再送・輻輳制御含む)は非常に大きいため今回は見送り。
      ARP/IPv4/ICMP/UDP/DHCP/DNSで「本物のネットワーク往復」自体は証明済み
- [ ] Wi-Fi 対応 (ドライバ + wpa 相当のスタック)
- [ ] Bluetooth 対応

### M7 — ユーザーモード基盤 (リング3実行・システムコール・ELFローダー 完了)
- [x] GDT にユーザーコード/データセグメント追加 (`USER_CODE_SELECTOR`/`USER_DATA_SELECTOR`、
      RPL=3込み) + TSS.RSP0 — リング3タスクへの割り込み/トラップ時にCPUが自動的に
      切り替える先のカーネルスタックを指す。スケジューラがコンテキストスイッチの
      たびに更新 (`gdt::set_kernel_stack`)。カーネルタスクは特権変化が無いため無関係
- [x] システムコールゲート (`int 0x80`、ベクタ0x80、DPL=3の割り込みゲート) — 独自ABI
      (rax=番号、rdi/rsi=引数、rax=戻り値)。SYSCALL/SYSRET命令ではなく既存の
      共通割り込みスタブ/TrapFrame機構にそのまま乗る設計。`kernel/src/syscall.rs`に
      SYS_WRITE(1)・SYS_EXIT(0)を実装
- [x] プロセスごとのアドレス空間 (`memory::paging::AddressSpace`) — 新規PML4を確保し、
      ブート時のページテーブルの上位半分(カーネル+HHDM)をコピーして共有、下位半分は
      ELFローダー用に空のまま。`map`/`unmap`/`translate`は`map_in`/`unmap_in`/`translate_in`
      (明示的なPML4引数を取る版)の薄いラッパーとして再実装、既存呼び出し元は無変更
- [x] ELF64ローダー (`kernel/src/elf.rs`) — 静的リンクのET_EXECバイナリのみ対応
      (動的リンク/PIE/再配置は無し)。PT_LOADセグメントをページ確保・マッピングし、
      ファイル内容をコピー(1バイトずつではなくページ単位で変換して効率化)
- [x] スケジューラのCR3切り替え + リング3プロセス生成 (`sched::spawn_user`) —
      タスク構造体に`cr3`・専用カーネルスタック頂上・`AddressSpace`所有権を追加。
      毎ティック、次に実行するタスクのCR3が現在と違えばロードし直し、TSS.RSP0も更新
      (無駄なTLBフラッシュを避けるため変化した時だけ切り替え)。`sched::exit_current`で
      SYS_EXIT時にタスクをキューから外し、次のタスクへ切り替える
- [x] 最小ユーザーランドテストプログラム (`userland/init/`) — カーネルのワークスペースとは
      独立したスタンドアロンcrate(専用リンカスクリプト、ロードアドレス0x400000固定のET_EXEC)。
      `int 0x80`でSYS_WRITE→SYS_EXITを呼ぶだけの最小限の内容。`tools/build.sh`が先に
      このバイナリをビルドし、`USERLAND_INIT_ELF`環境変数経由でカーネルに`include_bytes!`
      で埋め込まれる (`kernel/build.rs`がデフォルトパスへのフォールバックも用意)
  - 実機確認: QEMUで`elf: init process loaded, entry=0x400000, ...` →
    `[user] Hello from moon OS userland (ring 3)!` (実際にリング3から`int 0x80`
    システムコール経由でカーネルに文字列を渡して出力) → `syscall: user task exited
    with code 0` → 既存のカーネルタスク(task A/B)がそのまま正常続行、を確認。
    10億回以上のタスク反復にわたり例外/パニック無しの安定動作も確認済み
- [x] moon OS ネイティブアプリの実行形式定義 (`.mapp`、`kernel/src/pkg.rs`) — マジック
      "MOAP"・フォーマットバージョン・名前/バージョン文字列・ELF64バイナリ本体を
      持つ単純なコンテナ形式。圧縮/複数ファイル同梱/署名はまだ無い(今後の課題)。
      `pkg::build`/`pkg::parse`で往復可能、`kernel/src/process.rs`が
      ELFローダー+`AddressSpace`+`sched::spawn_user`を1関数にまとめ、
      パッケージマネージャー・ファイルマネージャー・Moon Storeが同じ経路で起動する
- [x] パッケージマネージャー — 起動時に`userland/init`と`userland/counter`(2個目の
      ユーザーランドテストアプリ、ループしながら`tick 0`〜`tick 4`を出力)を`.mapp`化して
      RAMFSの`/apps/`に「インストール」。ターミナルに`pkg list`/`pkg run <name>`
      コマンドを追加 (`kernel/src/gui/widgets/terminal.rs`)
- [x] ファイルマネージャー (GUI、`gui/widgets/files.rs`) — RAMFSの中身を一覧表示、
      `.mapp`パッケージをダブルクリックで新規リング3プロセスとして起動
      (シングルクリックでの選択はQEMU実機で確認済み。ダブルクリック検出自体は
      同じ`pkg::run`経路を使う単純なtick差分ベースの実装だが、QEMUモニター経由の
      クリック注入では確実な二重クリック再現ができず、今回はターミナル経由の
      `pkg run`と起動時の自動実行の2経路で実際の起動は確認済み)
- [x] Moon Store (GUI、`gui/widgets/store.rs`) — インストール済み`.mapp`パッケージの
      一覧+ワンクリック起動。正直な制約: ネットワーク経由の実リポジトリはまだ無く、
      「ストア」と「インストール済み」が同じRAMFSの中身を指す(将来のダウンロード/
      インストールフローの土台となるUIシェル)

### M8 — 互換レイヤー (第一歩 完了)
- [x] EXE 互換レイヤー: PE32+ ローダー (`kernel/src/pe.rs`) — DOS/NT/COFF/オプション
      ヘッダーとセクションテーブルを解析(自作テストバイナリに限らず、well-formedな
      x86_64 PE32+イメージ全般に対応する汎用パーサー)、セクションを新規`AddressSpace`
      にマッピング。Win32 API サブセット: インポートディレクトリテーブルを解析し、
      `KERNEL32.DLL`の`ExitProcess`/`WriteConsoleA`のみ対応 — Win64呼び出し規約
      (rcx/rdx/r8/r9)をmoon OS独自の`int 0x80`ABI(rdi/rsi)に変換する極小サンクを
      カーネル内のnaked関数テンプレートから実行時にプロセスのアドレス空間へコピーし、
      IAT(Import Address Table)を書き換えて解決。実際のWindowsクロスツールチェーンが
      無い環境のため、テスト用EXEはNASMで手書きアセンブルしPythonスクリプトで
      PE32+ヘッダーを構築(`tools/pe_test/`) — objdumpで正当なPE32+/インポート
      テーブルとして認識されることを確認済み。QEMU実機でロード→
      `WriteConsoleA`経由の文字列出力→`ExitProcess`による正常終了までEnd-to-Endで
      動作確認(段階的拡張の第一歩。実際のWin32アプリを動かすには対応API関数を
      大幅に増やす必要があり、今回はローダー本体とサンク機構の実証に留める)
- [x] APK 互換レイヤー: ZIPコンテナリーダー + AXMLヘッダー検証 (`kernel/src/apk.rs`) —
      APKはZIPアーカイブなので、End Of Central Directoryレコードの後方探索、
      セントラルディレクトリ/ローカルファイルヘッダー解析を実装し、
      `AndroidManifest.xml`を取り出してAndroid Binary XML(AXML)の
      ResChunk_header(type/headerSize/size)を検証。正直な制約:
      DEFLATE展開は未実装(STOREDのみ対応 — 実際のAPKはほぼ全てDEFLATE圧縮のため、
      本物のAPKのマニフェストはまだ読めない)、AXMLの文字列プール/要素ツリー解析は
      未実装、Dalvik/ARTバイトコード実行は「パーサーを増やす」話ではなく実質的な
      Androidランタイム統合が必要なため今回は範囲外 — テスト用APK
      (`tools/apk_test/`、Python`zlib.crc32`で正しいCRCを計算し手書きで構築、
      Pythonの`zipfile`モジュールで独立検証済み)でQEMU実機確認

補足: EXE/APKレイヤーはいずれも「本物の実行ファイル形式を正しく解析できる
ローダー」という第一歩であり、実用的な互換性(実際のexe/apkを動かす)には
Win32 API/Androidランタイムの大幅な拡張が今後も必要。詳細は各モジュールの
doc commentに正直に記載。

### M9 — マルチメディア/その他
- [ ] オーディオスタック (AC97/HDA ドライバ + ミキサー)
- [ ] Web ブラウザー (自作 or 既存エンジン連携の方針決定)
- [ ] AI アシスタント (OS 統合)

### M10 — 次世代OS化 (進行中)

「OS風のデモ画面」から「毎日使いたくなる次世代OS」への全面改善。以下は
このパスで完了した項目 (QEMU実機で確認済み):

- [x] ウィンドウマネージャー刷新 (`gui/window.rs`, `gui/mod.rs`):
      タイトルバーの最小化/最大化/閉じるボタン、リサイズグリップ、
      エッジスナップ (上=最大化、左右=半分幅) のプレビュー、ドロップシャドウ、
      開閉時のフラッシュアニメーション。最小化→ドックアイコンクリックでの
      復元、フォーカス時のz-order挙上、グロー枠/コーナーブラケットを実機確認。
      ドラッグ/リサイズ自体はクリック検知と全く同じ座標更新ロジックだが、
      QEMUモニター経由の合成マウスイベントでの自動検証は入力イベント欠落が
      多く決定的な確認はできなかった (下記のバグ修正参照) — 実機/実マウスでの
      手動確認を推奨。
- [x] マウスカーソル刷新 (`gui/cursor.rs`): 手続き型の矢印/手カーソルを新規実装、
      クリック可能要素上でのホバー切り替えを実機確認。
- [x] デスクトップアニメーション (`gui/desktop.rs`): 実RTC時刻に基づく昼夜
      グラデーション、太陽/月の実位置移動、星の明滅、視差雲。浮動小数点数は
      未使用 (コンテキストスイッチでFPU/SSE状態を保存しないため、整数演算のみ)。
- [x] RAMFS本物のディレクトリ階層 (`fs/ramfs.rs`): `mkdir`/`rmdir`/`rename`/
      `copy`/`list_dir`。
- [x] ファイルマネージャー刷新 (`gui/widgets/files.rs`): リスト/グリッド表示、
      右クリックメニューからのコピー/切り取り/貼り付け/削除(ゴミ箱`/.Trash`へ
      移動)/新規フォルダ、検索、パンくず(ルートへジャンプ)、複数選択。
      リネームは実際にファイルを移動するが、名前入力UIは未実装のため
      `.renamed`サフィックス付与という簡易な形 (正直な簡略化であり偽実装ではない)。
- [x] フレームバッファのダブルバッファリング (`framebuffer.rs`):
      これまで各描画呼び出しが直接ハードウェアフレームバッファに書き込んで
      おり、非アトミックな複数回描画の途中状態がQEMUのscreendumpで実際に
      「デスクトップ背景だけ」「一部のウィンドウだけ」等の破れたフレームとして
      観測された。オフスクリーンバックバッファ + 1回の`present()`一括転送に
      変更し解消。副作用としてカーネルヒープを4MiBから32MiBに拡張
      (バックバッファだけで約3.9MiB消費するため)。
- [x] PS/2マウスドライバのパケット同期バグ修正 (`drivers/mouse.rs`):
      3バイトパケットの先頭バイトを、パケット全体を組み立てた後にしか
      検証していなかったため、1バイトのずれ(欠落/重複)が起きると
      以後永久にフレーミングがずれ、カーソルが無反応になったまま復帰しない
      という実バグがあった。バイト0候補ごとに同期ビットを検証し、無効なら
      進めずに次のバイトへ、というリシンク方式に修正。オーバーフロービット
      (bit6/7) が立ったパケットを破棄する防御も追加。
- [x] キーボードドライバに Shift キー対応を追加 (`drivers/keyboard.rs`):
      これまで大文字も `>` 等のシフト記号も一切入力できなかった (Altのみ
      状態追跡していた) ため、シェルのリダイレクト(`>`)はおろかファイル名の
      大文字すら打てなかった。左右Shiftの押下状態を追跡し、シフト時用の
      ASCIIテーブル(大文字+記号)を新規追加して切り替えるよう修正。
- [x] ターミナルの本物のシェル (`gui/widgets/terminal.rs`):
      RAMFS上で実際に動く`pwd`/`cd`/`ls`/`mkdir`/`touch`/`rm`/`cp`/`mv`/
      `cat`/`whoami`/`date`/`time`/`history`/`reboot`/`shutdown`を実装
      (カレントディレクトリの概念、`..`解決込み)。`echo`/`cat`は`>`による
      RAMFSへの出力リダイレクトに対応(実際にファイルへ書き込む)。
      コマンド名・パス名のタブ補完、矢印キーでのコマンド履歴も実装。
      `reboot`はキーボードコントローラのリセットライン(0xFE)、`shutdown`は
      QEMU固有のACPI PM1a_CNTポート(0x604)への書き込み — 後者は本物の
      ACPI FADT/DSDT解析を経ていないためQEMU専用と明記。QEMU実機で
      `pwd`/`ls`(実際のRAMFS内容`apps/`・`hello.txt`を表示)/`mkdir`
      (実際に`/`以下にディレクトリ作成)を確認済み。パイプ(`|`)は
      外部プロセスを繋ぐ実体が無い中で複数の組み込みコマンドの間を
      「それらしく」繋ぐだけになってしまうため、機能というより見せかけに
      近くなると判断し今回は意図的に対象外(正直な非対応として明記)。

以下はこのパスでまだ未着手/一部着手 (継続予定):
- [x] 通知センター (`gui/notifications.rs`): 画面右上、トップバー直下に
      トースト表示。パッケージのインストール/起動成功・失敗、ファイル
      マネージャーの削除(ゴミ箱移動)など実イベントのみを表示 — Wi-Fi/
      バッテリー/スクリーンショットのようなトーストは対応する実サブシステム
      が無いため対象外と明記。QEMU実機で起動時の実通知("installed 2
      bundled package(s)"/"launched init")を確認済み。
- [x] アクセントカラー設定 (`gui/theme.rs`): 5色のプリセットから選べる
      グローバルなアクセントカラー。ウィンドウの枠glow/フォーカス表示、
      ドック、トップバー、デスクトップウィジェットパネル、通知センター、
      ファイルマネージャーのビュー切り替えボタンなど、これまで個別に
      `(0x30, 0xE0, 0xFF)` をハードコードしていた箇所を全てこの一箇所の
      設定から読むように統一 — Settingsでクリックして色を変えると
      OS全体に即座に反映されることをQEMU実機で確認済み(Cyan→Magentaへの
      切り替えで全パネルの枠色が同時に変わることを確認)。
- [x] 電源操作 (`power.rs` + Settingsの Reboot/Shutdown ボタン):
      キーボードコントローラのリセットライン経由の再起動、QEMU固有の
      ACPI PM1a_CNT書き込みによるシャットダウンを実装(いずれもterminalの
      `reboot`/`shutdown`コマンドと共通の実装)。ボタンのクリック配線自体は
      アクセントカラー切り替えと全く同じディスパッチ経路だが、この2つの
      ボタンについては自動入力(QEMUモニター経由の合成マウスイベント)での
      実機クリック確認は入力イベント欠落により決定的な結果を得られなかった
      — 同じ経路を使うアクセントカラー切り替えは直前に実機確認できている
      ため配線自体への確証はあるが、reboot/shutdownの実行結果そのものは
      コードレビューによる確認にとどまる(正直な限界として記録)。
- [x] タスクバー刷新 (トップバー左の"moon OS"ロゴをクリック可能にし、
      本物のアプリランチャーメニューを追加 -- `gui/mod.rs`の`AppKind`):
      Terminal/Settings/Files/Moon Store/Notes/Calculator/Task Managerの
      7アプリをリスト表示、クリックで新規ウィンドウを開く(既に開いていれば
      重複させずフォーカスするだけ)。実行中アプリ一覧は既存の左ドックが
      既に担っている。Wi-Fi/Bluetooth/バッテリー/音量は実ハードウェア/
      ドライバが無いため引き続き対象外と正直に明記。QEMU実機でランチャー
      メニューが開くことは確認済みだが、メニュー項目クリックによる実際の
      アプリ起動は自動入力の欠落により決定的な実機確認はできなかった
      (コードパス自体はドックアイコン/アクセントカラー切り替え等、既に
      動作確認済みの他のクリック処理と全く同型 -- 正直な限界として記録)。
- [x] Moon Store の実メタデータ (`gui/widgets/store.rs`): 各パッケージの
      カテゴリ("Utility"/"Demo")と一行説明を実装(手書きの本物の説明文、
      ダミーのlorem ipsumではない)。検索ボックス、そして本物のUninstall
      ボタン(RAMFSから`.mapp`を実際に削除)を追加。星評価・レビュー数・
      「注目/ランキング」欄は実ユーザーが存在せず捏造データになるため
      意図的に対象外(正直な非対応として明記)。QEMU実機でUninstallボタンを
      押すと実際に一覧から消え、通知センターに"uninstalled counter"の
      トーストが出ることを確認済み。
      副産物として、ファイルマネージャーとMoon Store両方の検索ボックスが
      これまで`Window::handle_char`から一切配線されておらず(ターミナルにしか
      キー入力が届いていなかった)、検索欄が「表示だけで実際には入力できない」
      状態だったバグを発見・修正(`gui/window.rs`)。
- [x] ストレージ/ネット速度ウィジェット (`gui/desktop_widgets.rs`):
      RAMFS実使用量(全ファイルサイズ合計)、実際の送受信バイトカウンター
      (`net::traffic_totals`、`send_frame`/`poll_once`という実際の送受信
      経路でインクリメント)から100tickごとに実レートを計算し表示。
      QEMU実機で"RAMFS: 10 KiB used"のような実数値表示を確認済み。
      ToDoリストは固定パネル(非インタラクティブ)の枠組みでは編集操作を
      受け付けられないため単体ウィジェットにはせず、後述の追加アプリの
      「メモ」アプリ(実際に編集・保存できるテキスト)に統合する形に変更 —
      天気/GPU/音楽ウィジェットは実データソース/ハードウェアが無いため
      引き続き対象外。
- [x] 追加標準アプリ: 電卓・メモ・タスクマネージャーの3本を追加
      (アプリランチャーから起動)。
      - 電卓 (`gui/widgets/calculator.rs`): 整数演算のみの本物の四則演算。
        小数を意図的に非対応にしたのは手抜きではなく、このカーネルの
        コンテキストスイッチがFPU/SSE状態を保存/復元しないため(前述の
        `gui/desktop.rs`の方針と同じ)、浮動小数点を扱うと2つのタスクが
        同時にFPUに触れた瞬間に静かに破損する恐れがあるから。ゼロ除算は
        オーバーフロー同様チェックし"Error"表示にしてパニックさせない。
      - メモ (`gui/widgets/notes.rs`): 実RAMFSファイル`/notes.txt`に毎
        キー入力で自動保存されるプレーンテキストエディタ。ウィンドウを
        閉じてもFile Managerで`/notes.txt`として実際に見える。
      - タスクマネージャー (`gui/widgets/taskmanager.rs`): スケジューラの
        実際の現在のタスク一覧 (`sched::list_tasks`、新規追加) をID/
        kernel-or-user種別付きで表示。CPU%列は無し(スケジューラにタスク
        別CPU時間の計測機構がまだ無く、表示すれば捏造数値になるため)。
        「終了」ボタンも無し(スケジューラに外部から任意タスクを安全に
        終了させる仕組みがまだ無く、押しても何も起きないボタンは今回
        避けたいダミー操作そのものになるため)。
      ペイント/コードエディタ/アーカイブマネージャーは今回見送り:
      ペイントは実キャンバス+描画ツール一式が必要でこのパスの範囲を超える、
      コードエディタはシンタックスハイライト基盤なしではメモと実質的に
      同じものになってしまう、アーカイブマネージャーは既存のZIPリーダー
      (`apk.rs`、STOREDのみ対応)を書き込み/展開可能にする作業が必要 --
      いずれも正直な非対応として記録(ブラウザ/写真/動画/音楽/
      メディアプレイヤーは引き続きTCP+HTMLエンジン/画像コーデック/
      オーディオが無いため対象外)。
- [x] Alt+Tab (キーボードドライバ側とGUI側の切り替えロジックを実装、
      QEMU実機でフォーカスが実際に順送りされることを確認済み)
- [x] Moon AI (`gui/widgets/moon_ai.rs`、アプリランチャーから起動):
      ユーザーとの合意通り、実LLMではなくキーワード/パターンマッチの
      ルールベース実用アシスタント。対応コマンド: 7アプリの起動("open
      settings"等 -- 新設の`gui::request_open`経由でウィンドウマネージャーに
      委譲)、`search files for <name>`(実RAMFSを実際に検索)、メモリ/時刻/
      日付の照会(実`pmm`/`RTC`読み取り)、reboot/shutdown(`power`モジュール
      呼び出し)。一致しない入力には自由なそれらしい回答を作らず正直に
      「わからない」と返す。コード生成・画像生成・自由QAは意図的に対象外
      (ROADMAPに記載の通り)。QEMU実機でランチャーメニューへの追加表示は
      確認したが、メニュー経由でのMoon AIウィンドウ起動そのものは他の
      新規ランチャー項目と同様、自動入力の欠落により決定的な実機確認は
      できなかった(正直な限界として記録)。

## 現在の状態 (最終更新: 2026-07-21)

M0〜M6 (基本部分) が完了。QEMU上でプリエンプティブなラウンドロビンスケジューラ、
PCIバス列挙→AHCI→ATAPI経由での実ディスク(起動用ISO)セクタ読み込み、GUIでの
ウィンドウドラッグ/フォーカス切り替え/ターミナル操作に加え、RTL8139ドライバ経由で
DHCPによる実IPアドレス取得(QEMU SLIRP)、ゲートウェイへのICMP ping応答受信、
DNS問い合わせによる実際の名前解決(example.com)まで確認済み — Ethernet/ARP/IPv4/
ICMP/UDPを自前実装したスタックで本物のネットワーク往復が成立することを実証。

M7着手前に、デスクトップの見た目をダーク×月/宇宙テーマへ刷新(壁紙・上部バー・
左ドック・カレンダー/システムモニターの半透明ウィジェット。詳細はM5セクション
参照)。QEMU実機でライブ値(uptime/タスク数/DHCP取得IP/実RTC日付)が正しく
反映されることを確認済み。

M7 (ユーザーモード基盤) も完了。GDTにリング3セグメント+TSS.RSP0、DPL=3の
`int 0x80`システムコールゲート、プロセスごとの`AddressSpace`(独自PML4、
上位半分はカーネル/HHDMと共有)、ELF64ローダー、スケジューラのCR3切り替えを
実装し、独立クレートの最小ユーザーランド(`userland/init`)を実際にリング3で
実行 — `int 0x80`経由でカーネルに文字列を渡して出力させ、SYS_EXITで正常終了、
既存のカーネルタスクがそのまま継続することをQEMU実機で確認済み(10億回以上の
タスク反復にわたり例外/パニック無し)。

M7 の残り(ネイティブアプリ実行形式・パッケージマネージャー・ファイルマネージャー・
Moon Store)も完了。`.mapp`パッケージ形式、2個目のユーザーランドテストアプリ
(`userland/counter`)、ターミナルの`pkg list`/`pkg run`コマンド、RAMFSブラウザの
ファイルマネージャー、ローカルカタログのMoon Store GUIをQEMU実機で確認 —
ターミナルから`pkg run counter`を実行して新規リング3プロセスが実際に起動し
(`tick 0`〜`tick 4`→`counter app done`を出力して正常終了)、既存タスクが
継続することを確認済み。

M8 (互換レイヤーの第一歩) も完了。PE32+ローダー(`kernel/src/pe.rs`)がDOS/NT/
COFF/オプションヘッダーとセクションを解析し、`KERNEL32.DLL`の`ExitProcess`/
`WriteConsoleA`の2関数についてWin64呼び出し規約→moon OS独自`int 0x80` ABIへの
変換サンクを実行時合成、IATを書き換えて解決。Windowsクロスツールチェーンが
無い環境のためテストEXEはNASM手書き+Pythonパッカーで構築 — QEMU実機で
ロード→`WriteConsoleA`出力→`ExitProcess`終了までEnd-to-End確認済み。
APK側はZIPコンテナリーダー+AndroidManifest.xmlのAXMLヘッダー検証
(`kernel/src/apk.rs`)を実装、実機確認済み。DEFLATE展開・AXML要素ツリー解析・
実際のDalvik/ART実行はいずれも今後の課題として正直に文書化(詳細はM8セクション
参照)。
M10 (次世代OS化) も大部分が完了。ウィンドウマネージャー刷新(ボタン/リサイズ/
スナップ/シャドウ/アニメーション)、手続き型カーソル、実時刻駆動の昼夜デスクトップ、
RAMFSディレクトリ階層、ファイルマネージャー実操作、通知センター、OS全体の
アクセントカラーテーマ、Reboot/Shutdown、Moon Storeの実メタデータ/検索/
アンインストール、ターミナルの本物のシェル、ストレージ/ネット速度ウィジェット、
アプリランチャー(Moonボタン)、電卓/メモ/タスクマネージャーの3新規アプリ、
ルールベースのMoon AIアシスタントを実装 — 開発中に見つかった実バグも複数修正
(フレームバッファのダブルバッファリング化、PS/2マウスのパケット同期、
ファイルマネージャー/Moon Storeの検索ボックスが実は一切キー入力を受け付けて
いなかった配線漏れ)。詳細と各項目の実機確認状況(および自動入力の限界により
確認しきれなかった項目の正直な記録)はM10セクション参照。

残っているのは Paint/コードエディタ/アーカイブマネージャーの3アプリ、
Wi-Fi/Bluetooth/バッテリー/音量(実ハードウェア/ドライバ待ち)、そして
M9 (オーディオ/ブラウザ) や M7残りの発展(実アプリ形式の署名・依存解決)、
EXE/APKレイヤーの拡張など。詳細は各コミットログおよびREADME.mdのビルド手順を
参照。

## 2026-07-22 追記: 永続ストレージ、カーソル感度、ログイン画面の見直し

ユーザーから「スキーみたいにカーソルが流れる」「ビジュアルOSじゃなくて本物の
OSにしてほしい」「ログインはパスワードだけでいいようにして」の3点フィードバックを
受けて対応。

- **マウス感度** (`kernel/src/gui/mouse_speed.rs`): QEMU/VirtualBoxの相対PS/2
  マウスは実機よりずっと大きいdx/dyを報告するため、カーソルが意図以上に
  滑っていた問題に対し、Slow(35%)/Normal(60%, 既定)/Fast(100%)の実スケーリングを
  導入。設定アプリに新しい行として追加し、クリックで切り替え可能(既存の
  Lang/Accent行と同じ配線パターン)。QEMU実機で起動→デスクトップ到達→
  Settingsに"Mouse speed: Normal"が表示されることを確認済み。
- **ログイン画面の見直し** (`kernel/src/gui/login.rs`): 日常のログインはパスワード
  欄のみ("Welcome back, <user>"の固定ラベル表示)にし、別アカウントへの
  ログインは"Switch user"リンクから旧来の2欄フォームを呼び出す方式に変更。
  Sign Upは従来通り。QEMU実機でパスワードのみの画面が表示され、デフォルト
  パスワードの入力でデスクトップに到達することを確認済み。
- **永続ストレージ** (`kernel/src/fs/persist.rs` + `drivers/ahci.rs`の
  `ata_read_sector`/`ata_write_sector`追加): 「本物のOSを作って」への最も
  具体的な回答として、RAMFSが起動の度に消えてしまう(=何を作っても残らない)
  問題に対応。第2のAHCIポートに繋いだ本物のSATAディスクへ、RAMFSツリー全体を
  定期的(約5秒毎)・シャットダウン/リブート時にスナップショット保存し、
  起動時に読み戻す。フォーマットは自前のシンプルな
  「スーパーブロック(マジック+長さ+CRC32) + [パス長, パス, データ長, データ]の
  繰り返し」で、本物のブロック単位ファイルシステムではないと明記した上での
  実装(コード内コメント参照)。`tools/run.sh`が`build/moon-os-disk.img`という
  永続ディスクイメージを自動作成してAHCIコントローラの別ポートに接続する。

  QEMU実機で以下を確認済み: (1) 起動時にSATAディスクを検出し
  `fs::persist`が使用開始, (2) ターミナルで`touch persisted.txt`を実行, (3) 約
  6秒後に自動保存(シリアルログで"saved 6 file(s), 9853 bytes"を確認),
  (4) QEMUプロセスをkillして同じディスクイメージで新規プロセスとして再起動,
  (5) 起動ログに"restored 6 file(s) from disk snapshot"と表示され、
  `/etc/passwd`もそのまま復元されデフォルトパスワードでのログインが成功 —
  ファイル・アカウントがリブートを跨いで実際に残ることを証明。

## 2026-07-22 追記(2): M11残タスク完了 + 本物のTCP/HTTP + インターネットブラウザ

ユーザーから「残りのM11タスク(Moon Store、通知センター、設定拡張)も続けて。
あと、インターネットブラウザがほしい」との依頼。

**M11残タスク:**
- Moon Store: インストール済みパッケージから実際に存在するカテゴリだけを
  集めた実カテゴリフィルタ("All"含む)、カテゴリ→名前の決定的ソート。
  ダウンロード進捗バーやレビュー機能を依然として追加しない理由(測る実体が
  ない)をコメントで明記。
- 通知センター: 各通知に実カテゴリ(Files/Packages/System)を付与しベルの
  ドロップダウンでフィルタ可能に、本物の"Clear all"、トースト表示だけを
  抑制する(履歴には残る)Do Not Disturbトグルを追加。
- 設定: 実DHCPリースIP(または"no lease")を表示するNetwork行、Do Not
  Disturbトグル行を追加。

**TCP/HTTPクライアント (`kernel/src/net/tcp.rs`, `http.rs`):**
これまでUDP/ICMPのみだったネットワークスタックに、クライアント専用の
本物のTCP(3ウェイハンドシェイク、実チェックサム、順序通りのACK、4ウェイ
クローズ -- 再送/輻輳制御/並び替えなし、1リクエスト分のブロッキング利用に
限定した正直なスコープ)と、その上のHTTP/1.1 GETクライアントを追加。

実装中に本物のバグを2つ発見・修正:
1. `ipv4::send`が宛先IPを常に直接ARPしていたため、同一サブネット外(=実際の
   インターネット上のホスト全部)への送信が絶対に成立しなかった -- デフォルト
   ゲートウェイ経由でルーティングするよう修正。
2. Ethernetフレームの最小長パディング(60バイト)を、IPヘッダの実際の
   Total Lengthで切り詰めずにそのまま上位層へ渡していたため、データを伴わない
   短いTCPセグメント(SYN-ACKやFIN)にゼロパディングが「ペイロード」として
   誤混入していた -- `ipv4::parse_header`が`total_len`を持つようにし、
   `handle()`でそこまでに切り詰めるよう修正。

QEMU実機で確認: 実際のDNS解決→3ウェイハンドシェイク→GETリクエスト送信→
レスポンス解析まで一貫して成功(ステータスコードとボディ長を正しく取得)。
このサンドボックス環境自体のネットワーク/DNSポリシーにより実際のステータス
コードは変わりうるが、プロトコル実装そのものは実サーバー相手に動作確認済み。

**Browser アプリ (`kernel/src/gui/widgets/browser.rs`):** アドレスバーに
入力したURLを実DNS解決→実TCP接続(`https://`は下記TLS 1.3経由)→実HTTP GETで
取得し、HTMLタグをプレーンテキストへ変換して表示する、正真正銘ネットワーク
越しに取得した内容を映す最小ブラウザ。レイアウト/CSS/画像/JavaScriptは一切
なし -- 「タグを剥いだテキスト表示」であり本物のレンダリングエンジンでは
ないと明記。`AppKind::Browser`としてランチャーメニュー/検索/Moon AIの
"open browser"コマンドから開ける。

QEMU実機で確認: ビルド/clippyともにクリーン、デスクトップは正常に起動し
Moonメニューに"Browser"が一覧表示されることを確認。メニュー項目クリックでの
実際の起動そのものは、このセッションで繰り返し観測されている合成マウス
イベントの不正確さ(相対PS/2デバイス特有の問題、以前のセクションでも記録
済み)により決定的に確認できなかった -- ウィジェットの配線パターンは
Store/Notes/Settingsなど既にクリック確認済みの他アプリと完全に同一であり、
コードレビューと構造的な一致による確認に留めた、正直な記録。

## 2026-07-22 追記(3): HTTPS対応(本物のTLS 1.3、ゼロ依存の自前実装)

ユーザーから「https対応してよ。もっと未来に通用するOSにしようと努力しない?」
との依頼。外部crateへの依存なしで、RFC/FIPS仕様書から暗号プリミティブを
一つずつ書き起こし、その上に本物のTLS 1.3クライアントを実装した。

**暗号プリミティブ (`kernel/src/crypto/`):** SHA-256(FIPS 180-4)、
HMAC-SHA256(RFC 2104/FIPS 198-1)、HKDF-Extract/Expand + TLS 1.3の
HKDF-Expand-Label(RFC 5869 / RFC 8446 §7.1)、ChaCha20(RFC 8439、IETF版・
32bitカウンタ)、Poly1305(RFC 8439、5リム×26bit基数)、AEAD_CHACHA20_POLY1305
(RFC 8439 §2.8、タグ比較はタイミング攻撃を避けるためconstant-time)、
X25519(RFC 7748、Montgomeryラダー、5リム×51bit基数の体演算、フェルマーの
小定理による逆元計算)。それぞれ独立した`self_test()`を持ち、カーネル起動時
に`crypto::self_test()`が全7個を実行して結果をシリアル出力する。

各プリミティブは`no_std`カーネルに組み込む前に、`std`環境の別ハーネスで
Python(`hashlib`/`hmac`標準ライブラリ、`pycryptodome`)による独立実装と
突き合わせてテストベクタを検証した -- RFC記載の64桁16進数を手で書き写すと
一文字drop/off-by-oneのような転記ミスを何度も起こしたため(実際に発生し、
都度検出・修正した)、最終的に人力の書き写しを信用せず全ベクタを
スクリプトで機械的に検証する方式に切り替えた。

**TLS 1.3クライアント (`kernel/src/net/tls.rs`):** ClientHello送信
(X25519 key_share拡張含む)からServerHello受信、鍵スケジュール
(early_secret→handshake_secret→master_secret→各traffic_secret)導出、
AEADレコード層での暗号化送受信、サーバーFinishedのHMAC検証まで、
本物のTLS 1.3ハンドシェイクを実装。`net::tcp`に`read_exact(n)`
(指定バイト数ちょうど届くまでブロックする読み込み)を追加し、TLSレコード
ヘッダ→宣言された長さ分のボディという可変長パースを可能にした。

実装中に本物のバグを2つ発見・修正:
1. 鍵スケジュール冒頭のearly_secretで、saltを1バイトのゼロにしていた
   (RFC 8446 §7.1が要求するのは32バイトのゼロsalt) -- テスト前のコード
   レビューで発覚し修正。
2. アプリケーショントラフィック鍵を、クライアント自身のFinishedメッセージ
   まで含めたtranscriptから導出していた -- RFC 8446 §7.1ではサーバーの
   Finishedまでのtranscriptから導出しなければならない。QEMU実機での
   ハンドシェイク自体は成功するのに最初の復号読み込みだけがAEAD認証エラー
   で無言失敗する、という症状から特定し、サーバーFinished直後の
   transcriptを別変数に確保して修正。

QEMU実機で確認: 全7暗号プリミティブのself-testが実機ブート時に全て成功、
実際のTLS 1.3ハンドシェイク→AEAD復号→HTTPレスポンス解析まで一貫して成功
(プレーンHTTPと同一のステータス/ボディ長を取得、このサンドボックスの
ネットワークegressポリシーによる403も両方一致)。

**正直な限界(重要):** この実装は証明書チェーンの検証を一切行わない --
サーバー証明書自体を要求も検証もせず、X25519鍵交換だけで暗号化する。
つまり受動的盗聴に対しては本物の暗号化だが、能動的な中間者攻撃(偽の
サーバーが割り込んでくるケース)に対しては無防備であり、ブラウザが
表示する"https://"は「暗号化されている」以上の保証をしない。証明書検証
(X.509パース、署名検証、信頼ストア)を実装するのはこのTLSクライアント
そのものと同規模以上の作業であり、今回のスコープには含めていない --
コード内(`tls.rs`のモジュールコメント)にも同じ内容を明記している。
